specVersion: "v1beta"
serviceId: "bird-forecast"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bird-forecast-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
